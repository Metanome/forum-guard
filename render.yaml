# render.yaml (Minimal for diagnostics)

services:
  - type: worker
    name: forum-guard
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: DISCORD_BOT_TOKEN
        sync: false # This must be set in the Render dashboard
